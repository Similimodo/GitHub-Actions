#!/usr/bin/env python3
import math

x = 3.14
y = -4
z = 9

# result = round(x) # Rounding it to the closest whole munber
# result = abs(y) #Absolute distance from zero
# result = pow(2,4) #Raised to power
# result = max(x,y,z) #Maximum value amongst many variables
result = min(x,y,z) #Minimum value amongst many variables

print(result)
print("****************")

print(math.pi)
print(math.sqrt(z)) #squareroot
print(math.ceil(x)) #Raises the number to the next whole number
print(math.floor(x)) #Lowers the number to the next whole number
